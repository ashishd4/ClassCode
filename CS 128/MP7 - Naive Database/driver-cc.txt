#include <iostream>

#include "db.hpp"
#include "db_table.hpp"

int main() {
  DbTable table;

  table.AddColumn(std::pair<std::string, DataType>("Name", DataType::kString));
  table.AddColumn(std::pair<std::string, DataType>("Age", DataType::kInt));
  table.AddColumn(std::pair<std::string, DataType>("GPA", DataType::kDouble));
  table.AddColumn(std::pair<std::string, DataType>("Nickname", DataType::kString));

  table.AddRow(std::initializer_list<std::string>({"Ashish", "19", "2.7", "Ashu"}));
  table.AddRow(std::initializer_list<std::string>({"Vihaan", "20", "8.8", "Baby"}));
  table.AddRow(std::initializer_list<std::string>({"Atishi", "18", "4.6", "Cute"}));
  table.AddRow(std::initializer_list<std::string>({"Divish", "12", "3.9", "Divu"}));
  table.AddRow(std::initializer_list<std::string>({"Harish", "50", "5.0", "EEEE"}));

  std::cout << table << std::endl;
  

  table.DeleteRowById(1);
  // table.DeleteRowById(1);

  std::cout << table << std::endl;
}
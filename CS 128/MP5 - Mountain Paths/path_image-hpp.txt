#ifndef PATH_IMAGE_H
#define PATH_IMAGE_H

#include <cstdlib>  // for size_t
#include <vector>

#include "color.hpp"
#include "grayscale_image.hpp"
#include "path.hpp"

using namespace std;

class PathImage {
public:
  // write behavior declarations here; define in path_image.cc.
  PathImage(const GrayscaleImage& image, const ElevationDataset& dataset);
  size_t Width() const;
  size_t Height() const;
  unsigned int MaxColorValue() const;
  const vector<Path>& Paths() const;
  const vector<vector<Color>>& GetPathImage() const;
  void ToPpm(const string& name) const;

private:
  vector<Path> paths_;
  size_t width_ = 0;
  size_t height_ = 0;
  vector<vector<Color>> path_image_;
  static const int kMaxColorValue = 255;

  static const int kRedR = 252;
  static const int kRedG = 25;
  static const int kRedB = 63;

  static const int kGreenR = 31;
  static const int kGreenG = 253;
  static const int kGreenB = 13;
};

#endif
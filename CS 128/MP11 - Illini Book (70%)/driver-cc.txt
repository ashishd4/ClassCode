#include "illini_book.hpp"

#include <iostream>

using namespace std;

int main() {
  IlliniBook book = IlliniBook("./example/persons.csv", "./example/relations.csv");

  string rs = "";
  vector<int> uins = {1, 2, 3, 6, 7, 8, 9, 11, 13, 15};

  for (int uin : uins) {
    std::cout << uin << " :  " << book.DistanceHelper(3, uin) << std::endl;
  }

  std::cout << std::endl;

  vector<int> steps = book.GetSteps(3, 1);

  for (int num : steps) {
    std::cout << num << std::endl;
  }

  std::cout << "Groups: " << book.CountGroups("124") << std:: endl;

  // std::cout << book.AreRelated(1, 2) << std::endl;
  // std::cout << book.AreRelated(3, 2) << std::endl;
  // std::cout << book.AreRelated(1, 9) << std::endl;
  //  std::cout << book.GetRelated(1, 2) << std::endl;
  //  std::cout << book.GetRelated(3, 2) << std::endl;
  //  std::cout << book.GetRelated(1, 9) << std::endl;
  //std::cout << book.CountGroups() << std::endl;
  //std::cout << book.CountGroups(std::vector<std::string>{"124", "128", "173" }) << std::endl;
  //  vector<int> steps = book.GetSteps(9, 1);
  //  for (int i = 0; i < (int)steps.size(); i++) {
  //     std::cout << steps.at(i) << std::endl;
  //  }
   //std::cout << book.AreRelated(1, 2, "124") << std::endl;
  //std::cout << book.GetRelated(1, 7, "128") << std::endl;
  return 0;
}
